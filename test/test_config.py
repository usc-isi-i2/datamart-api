# # Postgres config
# config = dict(DB = dict(
#     database = 'wikidata',
#     host = 'localhost',
#     port = 5433,
#     user = 'postgres',
#     password = 'postgres',
# ), STORAGE_BACKEND='postgres')

# SQL Server Config
config = dict(DB = dict(
    database = 'wikidata',
    host = 'localhost',
    port = 1433,
    user = 'sa',
    password = 'password!12',
), STORAGE_BACKEND='sql-server')
